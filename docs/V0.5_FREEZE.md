# V0.5 FREEZE

**Status:** `V0.5 FREEZE READY`  
**Date:** 2026-09-04

## Why V0.5 exists

Blind Test 02 on S-07 exposed silent short-value column shifts in basic EPS, diluted EPS and ROE. The frozen first-run artifact was preserved before ground-truth review. S-07 then became regression-only.

## Frozen repairs

1. Primary-table numeric values are assigned by left-to-right numeric-token order within a matched metric row: `current → comparison → disclosed change`.
2. A parsed primary row missing any of those three values is a blocking FAIL.
3. The expected primary-table schema is frozen. An entire missing metric row is also a blocking FAIL.
4. Downstream metric labels use canonical names so nearby explanatory prose cannot leak into Evidence labels.
5. V0.4 controls remain: source metadata inheritance, geometry-preserving PDF.js extraction, primary-page isolation, cross-page non-recurring total, YoY reconciliation, ratio percentage-point reconciliation and F-02 bridge validation.

## Regression evidence

GitHub Actions run `33877851946` established the value-level regression baseline on official PDFs:

- V0.4 fixture regression: 3/3 PASS
- S-05 real PDF regression: PASS
- S-06 real PDF regression: PASS
- S-07 real PDF regression: PASS

Artifact:
- id: `9938590749`
- digest: `sha256:ec315c56f2f1b5aa1da2e86d1890a5fbd90b621f428380b3c69fc28b60c92adb`

Final strict-schema run `33878158269` additionally verifies:

- all frozen primary rows are present for S-05, S-06 and S-07;
- every primary row contains current/comparison/disclosed-change values;
- canonical labels are emitted;
- an entirely absent primary schema is blocked rather than silently accepted.

## Frozen primary schema

- revenue
- attributable_np
- adjusted_np
- operating_cash_flow
- basic_eps
- diluted_eps
- roe
- total_assets
- attributable_equity
- non_recurring_total

## Governance boundary

- No Claims, Assumptions, Kill Criteria, Formula, Valuation or Decision rules were changed.
- S-06 and S-07 are regression-only.
- The failed S-07 first-run artifact remains immutable and must not be rewritten.
- V0.5 must not be altered based on the next blind-test document before its first-run artifact is saved.

## Next stage

Choose a genuinely unseen report as S-08, verify existence only, freeze its source metadata, then execute Blind Test 03 with no expected values encoded in the first-run harness. Save the first-run PDF hash and raw parser output before human ground-truth comparison.
